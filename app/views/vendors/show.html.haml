%h1= @outside_entity.name
  
%table
  %tr
    %th Date
    %th Amount

  - @transactions.each do |trans|
    %tr.transaction{ :class => cycle('odd', 'even') }
      %td.date= trans.executed_at.strftime '%m/%d'
      %td.amount= display_amount trans
